import React from "react";

export const RNG = (lowNumber, highNumber) => {
  /*
    What this does:
    Get a low and high number and generate a random number between them.
    If only one number is passed, assume that the low number is 0.
    */
  return <div>RNG</div>;
};
